# SSC Reference

Design notes for SecurityScorecard public reference sync and optional API integration.

Public SSC data must never be applied automatically; all changes must go through admin review.
