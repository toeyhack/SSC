from app.models import catalog_models  # ensure catalog models are imported so metadata is registered
from app.models import rule_models  # ensure rule engine models are imported so metadata is registered
from app.models import scan_models  # ensure scan engine models are imported so metadata is registered
